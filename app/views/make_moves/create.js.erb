  var current_player = '<%= @current_player_symbol %>'
  var is_winner = '<%= @is_winner %>'


  if( is_winner === "false" ) {
    $("div#<%= @space.number %>").text("<%= @current_player_symbol %>");
  } else {
    $("div#<%= @space.number %>").text("<%= @current_player_symbol %>");
    $('div#winner').text("Player " + current_player + " Wins");
    $('div.game_square').children('a').remove();

  };


